# P12.2 — Optional: scheduled reprocessing

P12.2 — Optional: scheduled reprocessing
If time allows in week 4-5, add a scheduled job (GitHub Actions cron, or a simple cron
job on the deployment VM) that checks ASF for new PROVISIONAL NISAR acquisitions over
our AOIs on a documented interval (e.g., daily) and reprocesses/appends new Change
Passports automatically. This is explicitly a stretch feature — do not let it delay
Phase 12.1's core deployment. Document clearly in the README whether this was
implemented or left as a described-but-not-built future step, and never claim it's
running live in the pitch unless it demonstrably is.

Acceptance criteria: if built, it is verifiably running (show a real log of at least
one automated reprocessing run); if not built, the README and pitch materials say so
honestly rather than implying an always-on live system that doesn't exist.
